import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
import axios from "axios";
import { BaseUrl } from "../../utils/BaseUrl";

const notificationsSlice = () => {
  return (
    <div>notificationsSlice</div>
  )
}

export default notificationsSlice